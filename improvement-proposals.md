# お友達図鑑 改善案の精査（実現可能性・工数）

作成日: 2026-09-03
対象: chara-collection（React + TypeScript + Vite / PWA / IndexedDB）
目的: 改善候補の実現可能性と工数を精査し、着手対象を絞り込む。

## 前提と見積もりの考え方

- 本アプリは Web 技術のみで完結し、ドメイン(純粋TS) / hooks(View-State) / components(UI) / persistence(IndexedDB) に分離済み。追加ライブラリは基本不要。
- 工数目安（1人・実装＋最小テスト＋デプロイ確認込みの相対規模）:
  - S = 半日以内 / M = 1〜2日 / L = 3日以上
- 「実現可能性」は技術的な難所の少なさ・既存設計への乗りやすさ。すべて Web 完結のため技術的リスクは総じて低い。

---

## 改善候補の精査

### A-1. 並び替え（登録順 / お気に入り度順 / 名前順）
- 概要: 一覧の並び順を切り替える。既定は現状どおり登録日時の新しい順。
- 触る箇所: `useCollection`（ソートキーの state と比較関数）, `CollectionView`（切替 UI）。ドメイン変更なし。
- 実現可能性: 高。並び替えは純粋関数で表現でき、テストも容易。
- 工数: S
- リスク: ほぼ無し。名前が空のキャラの扱い（末尾に寄せる等）を決めるだけ。
- 備考: お気に入り度順・名前順は A-2 と相性が良い。

### A-2. お気に入り度のソート表示・★の見た目強化
- 概要: 一覧カード/詳細でお気に入り度を視覚的に強調（ハート/★の数を明示）。A-1 の「お気に入り度順」と併せると効果的。
- 触る箇所: `CharacterCard`（度合い表示の追加）, `FavoriteLevelPicker`（表示専用モードの見た目強化。既にハート表示はある）, CSS(`global.css` / `tokens.css`)。
- 実現可能性: 高。表示のみでデータ変更なし。
- 工数: S
- リスク: ほぼ無し。見た目調整が中心。

### A-3. 一覧の表示切替（グリッド / リスト、写真大きめ）
- 概要: 一覧のレイアウトを切り替える。グリッド（現状）/ リスト（1列・情報多め）/ 大きめ写真。
- 触る箇所: `CollectionView`（表示モード state + クラス切替）, CSS。任意で表示モードを localStorage に保存。
- 実現可能性: 高。CSS 中心。
- 工数: S〜M（3モード＋設定保存まで入れると M 寄り）
- リスク: 低。レスポンシブ（横スクロールなし・要件7.6）の維持に注意。

### A-4. 対戦の演出強化
分割可能。効果音は不採用。

- (a) 自動再生: 一定間隔で自動 `advance()`。開始/停止トグル。
  - 触る箇所: `RankingBattleView`（setInterval + 状態管理）。`useRankingBattle` は変更ほぼ不要。
  - 実現可能性: 高 / 工数: S / リスク: 低（アンマウント時のタイマ解除に注意）。
- (b) リザルト画面: 優勝発表を専用の演出画面に（紙吹雪風アニメ等）。
  - 触る箇所: `RankingBattleView` + CSS（keyframes）。
  - 実現可能性: 高 / 工数: S〜M / リスク: 低。
- (c) アニメーション: 勝者ハイライト、ペア入場のトランジション。
  - 触る箇所: `RankingBattleView` + CSS。
  - 実現可能性: 高 / 工数: S〜M / リスク: 低。
- (e) ブラケット表示（勝ち上がり表）: トーナメント全体の進行を図で表示。
  - 触る箇所: `TournamentEngine`（現状「現在ペア」しか公開しないため、全ラウンドの組み合わせ・結果を保持/公開する拡張が必要＝ドメイン変更）, `useRankingBattle`, `RankingBattleView`, CSS。
  - 実現可能性: 中 / 工数: M〜L / リスク: 中（エンジン拡張の設計・既存プロパティテストとの整合）。

対戦演出の内訳まとめ:
- (a)+(b)+(c) はエンジン変更不要でまとめて M。まずここが着手しやすい。
- (e) はドメイン拡張が必要で M〜L。演出の中では最も重い。

---

## 優先度の提案（ROI 順）

1. A-1 並び替え + A-2 ★強化（S）: 日常操作の質を最小工数で底上げ。相互に補完。
2. A-4 (a)(b)(c) 対戦演出（M）: エンジン変更なしで「また使いたい」を作る楽しさ。
3. A-3 表示切替（S〜M）: 見やすさ向上。軽い割に体感が変わる。
4. A-4 (e) ブラケット表示（M〜L）: 演出の完成度は上がるがドメイン拡張が必要。後回し候補。

## まとめて着手する場合のイテレーション案

- イテレーション5「一覧の使い勝手」: A-1 + A-2（+ 任意で A-3）。工数 S〜M。低リスク・即効。
- イテレーション6「対戦を魅せる」: A-4 (a)(b)(c)。工数 M。エンジン変更なし。
- イテレーション7「対戦を魅せる（発展）」: A-4 (e) ブラケット表示。工数 M〜L。ドメイン拡張あり。

---

次アクション: 上記から着手対象を1つ（または1イテレーション）選べば、要件・設計・タスクに落として実装まで進めます。

---

# 追加要望の精査（2026-09-03 追記）

前提として、本アプリは要件3.8「Character データ・写真をいかなる外部サーバーへも送信しない」＝完全オフライン・端末内完結が土台。この前提に触れる案（特に AI 相関図）はそこを明記する。

## E-1. UI をもう少しおしゃれに
- 概要: パステル＋角丸の方向性は維持しつつ、余白・影・フォント・アクセント・カード質感・トランジションを磨く。トークン（`tokens.css`）調整で全画面に波及させる。
- 触る箇所: `tokens.css`（色・角丸・影・余白・フォント変数）, `global.css`, 各コンポーネントの微調整。構造変更は最小。
- 実現可能性: 高。既にデザインシステム（CSS カスタムプロパティ）が入っているので一括調整しやすい。
- 工数: S〜M（どこまで磨くか次第。トークン調整＋主要画面の仕上げで M）
- リスク: 低。要件7（44px タッチ領域・横スクロールなし・rem 追従）は維持する。
- 補足: 「おしゃれ」の方向性（大人可愛い/ポップ/ミニマル等）を決めると精度が上がる。参考イメージがあると早い。

## E-2. 一覧でニックネームを先頭に（名前とニックネームの表示順を逆転）
- 概要: 一覧カードでニックネームを主表示（大きく先頭）、名前を副表示に。ニックネーム未登録時は名前を主表示にフォールバック。
- 触る箇所: `CharacterCard`（表示順とスタイル）, CSS。データ変更なし。
- 実現可能性: 高。
- 工数: S
- リスク: ほぼ無し。要件2.5/2.6（名前・ニックネーム表示）の範囲内。未登録時のフォールバック方針だけ決める。

## E-3. 登録内容を増やす（項目案）
- 概要: 現状は 名前 / ニックネーム / メモ / お気に入り度 / 写真。ここに項目を追加。データ形状変更のため、読み出し時に既定値で埋める後方互換が必要。
- 触る箇所: `types.ts`（Character 拡張）, `CharacterValidator`, 登録/編集フォーム, 詳細表示, `IndexedDbCharacterStore`（旧データ正規化）。
- 実現可能性: 中（モデル変更を伴う）。
- 工数: 追加項目数に応じて M（数項目まとめて）。
- 項目案（相関図 E-5 のAIっぽい自動生成の材料にもなる）:
  - カテゴリ/ジャンル（アニメ・有名人・ポケモン 等。単一選択）
  - 作品名・出典（自由入力）
  - タグ（複数・自由）… 相関図のエッジ根拠に最適
  - 初めて知った日 / 出会った日（日付）
  - 二人の思い出メモ（既存メモと別枠の「エピソード」）
  - 二人それぞれの推し度（彼/彼女で別々の★）… 内輪ノリ向き
  - 関係性の自己申告（「ライバル」「相棒」等の任意ラベル）… 相関図の直接入力にも使える
  - カラーテーマ/イメージカラー（カードの縁取りに反映すると E-1 とも相乗）
- リスク: 中。項目を増やすほどフォームが重くなるので、必須は増やさず任意中心にする。

## E-4. 今日の相棒に「キャラからの一言」（完全ランダム）
- 概要: 相棒キャラのセリフ風メッセージを表示。
- 重要な論点: 「完全ランダム（開くたび変わる）」は現行の要件5.2「同一暦日内は選出結果を固定」と方針が競合する。相棒キャラ自体は日替わり固定のまま、セリフだけ毎回ランダムにするかを決める必要がある。
  - 案A（開くたびランダム）: セリフは非決定（Math.random）で毎回変わる。実装は最小。ただし「その日の相棒の言葉」が毎回変わる点をどう見るか。
  - 案B（その日固定）: 暦日＋salt＋キャラidから決定的に1つ選ぶ（既存の決定的選出と同じ思想）。再オープンで同じ。要件5.2 と整合的。
- 触る箇所: `dailyMessage.ts`（セリフのテンプレート集＋選択関数。案Bなら決定的、案Aなら rng 注入）, `useDailyGacha`（メッセージ生成呼び出し）, `DailyGachaView`（吹き出し表示）。
- 実現可能性: 高。BattleCommentator と同じ「テンプレート＋選択」パターンで作れる。
- 工数: S（案A/Bどちらも小さい。吹き出しUIを凝ると +S）
- リスク: 低。要件との整合のため案A/Bの選択だけ確定させる。おすすめは案B（アプリの決定的選出の思想と一貫）だが、要望どおり「完全ランダム」なら案A。

## E-5. キャラ相関図（新規画面）＋ 関係の自動生成
- 概要: 登録キャラをノード、関係をエッジで表示する相関図。関係は登録内容から自動生成。
- 最重要の論点: 「AIかなんかで自動」について。
  - 本アプリは完全オフライン・外部送信禁止（要件3.8）。クラウドAI（OpenAI 等の外部API）に登録内容や写真を送るのは、この核心前提と真っ向から衝突する。採用するとアプリのコンセプト（二人だけの内緒のコレクション・オフライン）を壊す。
  - したがって現実的な選択肢は次の3つ:
    - 選択肢1【推奨】ルールベースの自動生成（オフライン維持）: 追加項目（カテゴリ/作品名/タグ/イメージカラー等 = E-3）や既存項目の一致度で関係エッジを自動生成する。例「同じ作品→"共演"」「同じカテゴリ→"仲間"」「タグ共有→"つながり"」「お気に入り度が近い→"同格"」。"AIっぽい" 体験を外部送信なしで実現できる。工数 M〜L（相関ロジック＝純粋ドメイン + 相関図UI）。
    - 選択肢2 端末内の軽量モデル: ブラウザ内 ML（transformers.js 等）で類似度を計算。追加依存が大きくバンドル肥大・PWA配信重量化・iOS メモリ制約のリスク大。工数 L 以上。過剰。
    - 選択肢3 クラウドAI利用（要件3.8 を緩和）: 実現は可能だが、オフライン前提・プライバシー前提を捨てることになる。要件・設計の根本変更、APIキー管理、通信失敗時の挙動、コスト。工数 L 以上＋方針転換の意思決定が必要。
- 相関図の描画: 追加ライブラリなしでも SVG で自前描画可能（円環レイアウト＋線）。凝るなら力学レイアウト等でライブラリ検討（バンドル増）。
- 触る箇所: 新規ドメイン（関係推定＝純粋関数）, 新規 hook, 新規画面（相関図）, ナビゲーション追加。E-3 の項目追加が事実上の前提。
- 実現可能性: 選択肢1なら中。選択肢2/3は低〜中（前提衝突・重量）。
- 工数: 選択肢1で M〜L（E-3 とセット運用が現実的）。
- リスク: AI の解釈次第で大きく変わる。オフライン維持なら「ルールベースで自動生成」が最適解。要望の"AI"はここでは「登録内容から自動で関係を推定する仕組み」と読み替えるのが安全。

---

## 追加要望の優先度・進め方（提案）

- まず E-1（UIおしゃれ）+ E-2（ニックネーム先頭）+ E-4（今日の一言）: いずれも低リスク・小〜中工数で体験が目に見えて良くなる。1イテレーションにまとめられる。
- 次に E-3（登録項目追加）: 相関図の土台になるので E-5 の前に入れる。
- 最後に E-5（相関図）: E-3 の項目を材料にルールベース自動生成（オフライン維持）で実装。AI外部送信は要件3.8 と衝突するため、採用有無を明確に意思決定してから着手。

### 要確認（着手前に決めたいこと）
1. E-4 の一言は「開くたびランダム(案A)」か「その日固定(案B)」か。
2. E-5 の関係生成は「オフラインのルールベース(推奨)」か「クラウドAI（要件3.8 緩和）」か。
3. E-3 で追加する項目の確定（上の項目案から採用分を選ぶ）。

---

# ロードマップ（A候補 × E候補 合算 / 確定方針反映）2026-09-03

## 確定した方針（このロードマップの前提）
- E-3 登録項目追加は「出会った日（任意・詳細画面のみ表示）」＋「イメージカラー（パステルのプリセットから選択・既定は縁取りなし）」の2項目のみ。
- E-4 今日の相棒の一言は「案B: その日固定（暦日＋キャラidから決定的に選ぶ）」。要件5.2 と整合。
- E-5 相関図は「オフラインのルールベースで関係を自動生成」（外部AI/送信なし。要件3.8 維持）。関係の張り方など詳細ルールは後日決定。
- A-4 の効果音(d)は不採用。B 案（エクスポート/検索/圧縮 等）は今回スコープ外。

## 全候補の一覧（工数）
- A-1 並び替え（登録順/お気に入り度順/名前順）: S
- A-2 お気に入り度の見た目強化（★/ハート数の明示）: S
- A-3 一覧の表示切替（グリッド/リスト/大きめ）: S〜M
- A-4a 対戦の自動再生: S
- A-4b 対戦のリザルト画面: S〜M
- A-4c 対戦のアニメーション: S〜M
- A-4e 対戦のブラケット表示（勝ち上がり表）: M〜L（TournamentEngine 拡張＝ドメイン変更）
- E-1 UIをおしゃれに（トークン刷新・全画面波及）: S〜M
- E-2 一覧でニックネーム先頭（未登録時は名前フォールバック）: S
- E-3 登録項目追加（出会った日＋イメージカラー、縁取り反映）: S〜M（モデル変更・旧データ正規化あり）
- E-4 今日の相棒の一言（案B・その日固定）: S
- E-5 相関図（新画面＋ルールベース関係生成）: M〜L（E-3 が前提。詳細ルール未定）

## イテレーション分けの方針

### イテレーション5「見た目と使い勝手の底上げ」 — 低リスク・即効
- 含む: E-1（UIおしゃれ）, E-2（ニックネーム先頭）, A-1（並び替え）, A-2（★強化）
- 目的: 触る頻度の高い一覧まわりを、モデル変更なしで一気に良くする。
- 触る箇所: `tokens.css` / `global.css` / `CollectionView` / `CharacterCard` / `useCollection` / `FavoriteLevelPicker`。ドメイン変更なし。
- 想定工数: M（各 S を束ねる）。リスク: 低。
- 任意で A-3（表示切替）を含めてもよい（+S〜M）。まずは含めず、余力で追加が無難。

### イテレーション6「登録項目の拡張」 — 相関図の土台
- 含む: E-3（出会った日＝任意・詳細のみ / イメージカラー＝プリセット・既定縁取りなし）
- 目的: Character にメタ情報を足し、E-1 の縁取り演出と相関図(E-5)の材料を用意する。
- 触る箇所: `types.ts`（Character 拡張）, `CharacterValidator`（出会った日の任意＋妥当性、色はプリセット値の検証）, 登録/編集フォーム, 詳細表示, `IndexedDbCharacterStore`（旧データ正規化＝既定値で補完）, `CharacterCard`/CSS（イメージカラーの縁取り反映）。
- 想定工数: S〜M。リスク: 中（モデル変更・後方互換）。
- 備考: イメージカラーの縁取りは E-1 と相乗するため、5 の後に置くと仕上がりが良い。

### イテレーション7「今日の相棒に一言」 — 小さく楽しい
- 含む: E-4（案B・その日固定のセリフ、吹き出し表示）
- 目的: ガチャ画面に日替わり固定のキャラ一言を追加。
- 触る箇所: `dailyMessage.ts`（セリフのテンプレート集＋決定的選択。暦日＋キャラid＋saltから選ぶ）, `useDailyGacha`, `DailyGachaView`（吹き出しUI）。ドメインは純粋・決定的を維持（既存 Property の思想と一貫）。
- 想定工数: S。リスク: 低。
- 備考: 6 と独立。順序入替や 5 と同時でも可。単独 S なので差し込みやすい。

### イテレーション8「対戦を魅せる」 — エンジン変更なしの演出
- 含む: A-4a（自動再生）, A-4b（リザルト画面）, A-4c（アニメーション）
- 目的: ランキング対戦をショー化。TournamentEngine は変更せず UI/CSS 中心。
- 触る箇所: `RankingBattleView`（自動再生のタイマ制御、リザルト画面、トランジション）, `global.css`（keyframes）。`useRankingBattle` はほぼ不変。
- 想定工数: M。リスク: 低（タイマのクリーンアップに注意）。

### イテレーション9「キャラ相関図」 — 新画面・ルールベース自動生成
- 含む: E-5（相関図）＋ 任意で A-4e（ブラケット表示）
- 目的: 登録内容から関係を自動推定して相関図を表示（オフライン・外部送信なし）。
- 前提: イテレーション6（E-3 の項目）完了。関係の張り方など詳細ルールは着手前に別途決定。
- 触る箇所: 新規ドメイン（関係推定＝純粋関数）, 新規 hook, 新規画面（SVG 自前描画想定）, ナビゲーション追加。A-4e を含める場合は `TournamentEngine` 拡張。
- 想定工数: M〜L。リスク: 中（新画面・ドメイン追加・レイアウト）。

## 依存関係の要点
- E-5（イテレーション9）は E-3（イテレーション6）に依存。
- E-1 の縁取り演出は E-3 のイメージカラーがあると完成度が上がるが、E-1 自体は先行可能。
- 他は相互に独立。5→6→7→8→9 の順を基本としつつ、7 は任意の位置に差し込み可、8 は 9 と独立に前倒し可。

## 進め方の提案
- まずイテレーション5 を spec 化して実装（低リスク・即効で成果が見える）。
- 続けて 6（項目追加）→ 7（一言）→ 8（対戦演出）。
- 9（相関図）は 6 完了後、関係ルールを確定してから着手。